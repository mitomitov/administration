</div>
</div>
<div class="white-box">
    <div class="row">
        <div class="col-sm-12">
            <h3 class="box-title m-b-0">{{ $options['title'] }}</h3>
            @if (!empty($options['helper_box']))
                <p class="text-muted m-b-30 font-13">{{ $options['helper_box'] }}</p>
            @endif
        </div>
